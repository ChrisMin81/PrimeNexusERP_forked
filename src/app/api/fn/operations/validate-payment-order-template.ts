/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DialogWrapperPaymentOrder } from '../../models/dialog-wrapper-payment-order';

export interface ValidatePaymentOrderTemplate$Params {
      body?: DialogWrapperPaymentOrder
}

export function validatePaymentOrderTemplate(http: HttpClient, rootUrl: string, params?: ValidatePaymentOrderTemplate$Params, context?: HttpContext): Observable<StrictHttpResponse<DialogWrapperPaymentOrder>> {
  const rb = new RequestBuilder(rootUrl, validatePaymentOrderTemplate.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DialogWrapperPaymentOrder>;
    })
  );
}

validatePaymentOrderTemplate.PATH = '/resources/openapi/secureOnlineBankingApi/PaymentOrderService/validatePaymentOrderTemplate';
